# https://docs.docker.com/v1.11/engine/reference/commandline/run/
# -d   Run container in background and print contain
# -p [ports] Publish a container's port(s) to the host,  --restart="no"  Restart policy (no, on-failure[:max-retry], always, unless-stopped)

docker run -d -p 5000:80 --name overlord --restart always nginx 

# ip:5000